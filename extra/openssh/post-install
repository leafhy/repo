#!/bin/sh

# Generate host keys if they don't already exist.
ssh-keygen -A >/dev/null 2>&1

cat <<EOF
##########################################################################################

To resolve "no hostkey alg" add the following to /etc/ssh/sshd_config and or ~/.ssh/config

'HostKeyAlgorithms +ssh-rsa'
'PubkeyAcceptedAlgorithms +ssh-rsa'

PubkeyAcceptedKeyTypes is aliased to PubkeyAcceptedAlgorithms

##########################################################################################
EOF
