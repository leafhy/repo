#!/bin/sh -e

./configure \
    --prefix=/usr \
    --bindir=/usr/bin \
    --sbindir=/usr/bin \
    --enable-really-static

make
make install

# Move all bin's to singular dir.
mv -f "$1/bin/"*3g "$1/usr/bin"

ln -sf ntfs-3g "$1/usr/bin/mount.ntfs-3g"
ln -sf lowntfs-3g "$1/usr/bin/mount.lowntfs-3g"

# Pkg cleanup.
rm -rf \
     "$1/bin" \
     "$1/sbin" \
     "$1/usr/lib" \
     "$1/usr/share/doc"
